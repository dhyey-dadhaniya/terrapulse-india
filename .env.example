# Used by docker-compose for the OPTIONAL containerized Postgres service only.
# Not needed if you use native PostgreSQL on localhost:5432 (the project default).
# Copy to .env and set a password before: docker compose up -d postgres
DB_PASSWORD=your_password_here
